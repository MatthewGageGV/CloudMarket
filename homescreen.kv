# This file contains the layout/design for the home screen
#:import utils kivy.utils

<HomeScreen>:
    FloatLayout:
        Image:
            source: "Background.png"
            pos_hint: {"bottom": 1, "left": 1}
            size_hint: 1, 1.25
            id: logo
        GridLayout:
#            canvas:
#                Color:
#                    rgb: utils.get_color_from_hex("#39B3F2")
#                Rectangle:
#                    size: self.size
#                    pos: self.pos
            rows: 1
            pos_hint: {"top": .45, "left": 1}
            size_hint: 1, .25
            ImageButton:
                source: "BuyButton.png"
                on_press:
                    # Make a darker version of the image
                on_release:
                    app.change_screen("buy_screen")
            ImageButton:
                source: "SellButton.png"
                on_press:
                    # Make a darker version of the image
                    # self.source = "image.png"
                on_release:
                    app.change_screen("sell_screen")
        GridLayout:
            rows:1
            pos_hint: {"top": .9, "left": 1}
            size_hint: 1, .2
        Image:
            source: "CloudMarket.png"
            pos_hint: {"top": .8, "left": 1}
            size_hint: 1, .15
            id: logo
#        Label:
#            pos_hint: {"top": .6, "left": 1}
#            size_hint: 1, .1
#            text: "Time Remaining: " + str(root.counter)
#            id: title_label